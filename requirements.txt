# Full `pip freeze` of the development environment. evaluate.py itself needs only
# opencv-python, numpy and torch (verified by scripts/check_submission.py).
#
# torch/torchvision were built against CUDA 13.0. Those wheels live on PyTorch's own
# index, not PyPI, so the line below is required: without it `pip install -r
# requirements.txt` fails, because PyPI publishes 2.13.0 but never 2.13.0+cu130.
# The pins are written without the +cu130 local version so that pip takes the CUDA
# wheel where it is available and falls back to the PyPI build where it is not.
--extra-index-url https://download.pytorch.org/whl/cu130

certifi==2026.7.22
charset-normalizer==3.4.9
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
idna==3.18
ImageIO==2.37.4
Jinja2==3.1.6
kiwisolver==1.5.0
lazy-loader==0.5
lpips==0.1.4
lxml==6.1.1
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.4
opencv-python==5.0.0.93
packaging==26.2
pillow==12.2.0
pyarrow==25.0.0
pymupdf==1.28.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-docx==1.2.0
reportlab==5.0.0
requests==2.34.2
scikit-image==0.26.0
scipy==1.18.0
setuptools==78.1.0
six==1.17.0
sympy==1.14.0
tifffile==2026.7.31
torch==2.13.0
torchvision==0.28.0
tqdm==4.70.0
typing_extensions==4.15.0
urllib3==2.7.0
